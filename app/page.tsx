// TODO: Main chat UI
// - List threads in a sidebar (useQuery from convex/threads)
// - Display messages for the selected thread (useQuery from convex/messages)
// - Show tool logs inline under assistant messages (useQuery from convex/toolLogs)
// - Input box that calls sendMessage mutation
// - "New Thread" button that creates a thread and provisions a Daytona sandbox

export default function Home() {
  return (
    <main>
      {/* TODO: Implement chat UI */}
      <p>Convex Sandbox Chatbot — UI coming soon</p>
    </main>
  );
}
